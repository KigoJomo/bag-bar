import { FC } from 'react';

const Hero: FC = () => {
  return (
    <section className="bg-foreground flex flex-col items-center">
      <h1 className='lowercase'>bag bar</h1>
    </section>
  );
};

export default Hero;
